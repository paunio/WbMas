import React from "react";

const Header = () => {
  return (
    <header>
      <nav className="navbar navbar-dark bg-dark">
        <span className="navbar-brand">WebMas</span>
      </nav>
    </header>
  );
};

export default Header;
